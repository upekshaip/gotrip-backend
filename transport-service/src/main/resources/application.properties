spring.application.name=transport-service

server.port=8087

spring.datasource.url=${TRANSPORT_SERVICE_DB_URL}
jwt.secret=${JWT_SECRET}

server.max-http-request-header-size=65536
eureka.instance.prefer-ip-address=true